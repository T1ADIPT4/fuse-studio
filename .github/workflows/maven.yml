# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
          

    - ชื่อ: Paperspace Deploy
  
   # คุณสามารถปักหมุดไปยังความมุ่งมั่นที่แน่นอนหรือรุ่น
   # ใช้: Paperspace/deploy-action@e19c92caa3220cff5d5bf62f86832c0d87745612


  ใช้: Paperspace/deploy-action@v1.4
  กับ:
    # คีย์ API ของ Paperspace ค่าเริ่มต้นเป็น env.PAPERSPACE_API_KEY
    apiKey: # ไม่จําเป็น
    # รหัสโครงการที่การปรับใช้อาศัยอยู่ภายใต้
    โครงการรหัส: 
    # เส้นทางสัมพัทธ์ไปยังไฟล์การกําหนดค่าเพื่อปรับใช้
    configPath: # ไม่จําเป็น
    # ภาพคอนเทนเนอร์เสริมที่จะใช้สําหรับการปรับใช้
    รูปภาพ: # ไม่จําเป็น
          
